

public int searchAccount( int[25] number ) {       ------- can't initialize array as arg

        number = new int[15];

        for (int i = 0; i < number.length; i++ )
              number[i] = number[i-1] + number[i+1];
        return number;                             ------returns int[] needs to return int type
   }